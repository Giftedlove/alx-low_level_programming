#include "main.h"

/**
 * _abs - funtion that computes the absolute value of an integer
 * @x: value to be computed
 * Return: 0
 */
int _abs(int x)
{

	if (x < 0)
		return (-x);
	else
		return (x);
}
